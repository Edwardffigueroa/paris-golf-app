import React from 'react';

	import classes from './TeamList.module.css'

	const TeamList = (props) => {
	    return (
	    	<div className={classes.TeamList}>TeamList</div>
		)
	 }

	export default TeamList
