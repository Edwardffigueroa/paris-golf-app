import React from 'react';

	import classes from './CreateTeam.module.css'

	const CreateTeam = (props) => {
	    return (
	    	<div className={classes.CreateTeam}>CreateTeam</div>
		)
	 }

	export default CreateTeam
